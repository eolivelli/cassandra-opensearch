target/
*.iml
.idea/
.vscode/
*.log
.DS_Store

# A cluster created by examples/local-cluster/create-cluster.sh with the default -d.
/cluster/

# The README's quick start unpacks the tarball into the repository root.
/cassandra-opensearch-*/

# Bob AI assistant context — local tooling only, not for upstream.
.bob/

# PR description draft — contents go into the GitHub PR body, not committed upstream.
.pr-body.md
